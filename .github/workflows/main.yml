name: CI

on: [push, pull_request]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Echo a message
        run: echo "Running build for data-parser"
      - name: Simple build step
        run: echo "Build completed"